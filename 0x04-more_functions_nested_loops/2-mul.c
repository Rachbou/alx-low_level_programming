#include "main.h"

/**
  * mul - Entry point
  * multiplies two integers and returns the result.
  * @a: The first number.
  * @b: The second number.
  * Return: the multiple of a and b.
  */

int mul(int a, int b)
{
	int c;

	c = a * b;
	return (c);
}
